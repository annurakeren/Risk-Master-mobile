Pubspec.yaml:dependencies:  flutter:    sdk: flutter  http: ^1.2.1  flutter_secure_storage: ^9.0.0lib/main.dart:import 'package:flutter/material.dart';import 'package:flutter_secure_storage/flutter_secure_storage.dart';import 'core/theme.dart';import 'features/auth/login_page.dart';import 'features/dashboard/dashboard_page.dart';void main() {  runApp(const RiskMasterApp());}class RiskMasterApp extends StatelessWidget {  const RiskMasterApp({super.key});  @override  Widget build(BuildContext context) {    return MaterialApp(      title: 'Risk Master',      theme: AppTheme.theme,      debugShowCheckedModeBanner: false,      home: const SplashScreen(),    );  }}class SplashScreen extends StatefulWidget {  const SplashScreen({super.key});  @override  State<SplashScreen> createState() => _SplashScreenState();}class _SplashScreenState extends State<SplashScreen> {  @override  void initState() {    super.initState();    _checkAuth();  }  Future<void> _checkAuth() async {    const storage = FlutterSecureStorage();    final token = await storage.read(key: 'token');    if (!mounted) return;    if (token != null) {      Navigator.pushReplacement(        context,        MaterialPageRoute(builder: (_) => const DashboardPage()),      );    } else {      Navigator.pushReplacement(        context,        MaterialPageRoute(builder: (_) => const LoginPage()),      );    }  }  @override  Widget build(BuildContext context) {    return const Scaffold(      backgroundColor: Color(0xFF1E3A5F),      body: Center(        child: Column(          mainAxisAlignment: MainAxisAlignment.center,          children: [            Icon(Icons.security_rounded, size: 64, color: Colors.white),            SizedBox(height: 16),            Text(              'Risk Master',              style: TextStyle(                fontSize: 28,                fontWeight: FontWeight.bold,                color: Colors.white,              ),            ),            SizedBox(height: 8),            Text(              'SPK EDAS Keamanan Informasi',              style: TextStyle(fontSize: 13, color: Colors.white60),            ),            SizedBox(height: 40),            CircularProgressIndicator(color: Colors.white54, strokeWidth: 2),          ],        ),      ),    );  }}lib/core/theme.dart:import 'package:flutter/material.dart';class AppTheme {  static ThemeData get theme => ThemeData(        useMaterial3: true,        colorScheme: ColorScheme.fromSeed(          seedColor: const Color(0xFF1E3A5F),          brightness: Brightness.light,        ),        appBarTheme: const AppBarTheme(          backgroundColor: Color(0xFF1E3A5F),          foregroundColor: Colors.white,          elevation: 0,        ),        cardTheme: CardTheme(          elevation: 0,          shape: RoundedRectangleBorder(            borderRadius: BorderRadius.circular(12),            side: const BorderSide(color: Color(0xFFE5E7EB)),          ),        ),        inputDecorationTheme: InputDecorationTheme(          border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),          contentPadding:              const EdgeInsets.symmetric(horizontal: 14, vertical: 12),        ),      );}lib/core/api_client.dart:import 'dart:convert';import 'package:flutter_secure_storage/flutter_secure_storage.dart';import 'package:http/http.dart' as http;class ApiClient {  // Ganti dengan URL server Anda  static const String _baseUrl = 'http://127.0.0.1:8000/api';  static const FlutterSecureStorage _storage = FlutterSecureStorage();  static Future<String?> getToken() => _storage.read(key: 'token');  static Future<Map<String, String>> _headers({bool auth = true}) async {    final headers = {'Content-Type': 'application/json', 'Accept': 'application/json'};    if (auth) {      final token = await getToken();      if (token != null) headers['Authorization'] = 'Bearer $token';    }    return headers;  }  static Future<ApiResponse> get(String path) async {    try {      final res = await http.get(        Uri.parse('$_baseUrl$path'),        headers: await _headers(),      );      return ApiResponse.fromResponse(res);    } catch (e) {      return ApiResponse.error(e.toString());    }  }  static Future<ApiResponse> post(String path, Map<String, dynamic> body, {bool auth = true}) async {    try {      final res = await http.post(        Uri.parse('$_baseUrl$path'),        headers: await _headers(auth: auth),        body: jsonEncode(body),      );      return ApiResponse.fromResponse(res);    } catch (e) {      return ApiResponse.error(e.toString());    }  }  static Future<ApiResponse> put(String path, Map<String, dynamic> body) async {    try {      final res = await http.put(        Uri.parse('$_baseUrl$path'),        headers: await _headers(),        body: jsonEncode(body),      );      return ApiResponse.fromResponse(res);    } catch (e) {      return ApiResponse.error(e.toString());    }  }  static Future<ApiResponse> delete(String path) async {    try {      final res = await http.delete(        Uri.parse('$_baseUrl$path'),        headers: await _headers(),      );      return ApiResponse.fromResponse(res);    } catch (e) {      return ApiResponse.error(e.toString());    }  }}class ApiResponse {  final bool success;  final int statusCode;  final Map<String, dynamic>? data;  final String message;  const ApiResponse({    required this.success,    required this.statusCode,    this.data,    required this.message,  });  factory ApiResponse.fromResponse(http.Response res) {    final body = jsonDecode(utf8.decode(res.bodyBytes));    return ApiResponse(      success: res.statusCode >= 200 && res.statusCode < 300,      statusCode: res.statusCode,      data: body is Map<String, dynamic> ? body : null,      message: (body is Map ? body['message'] ?? '' : '').toString(),    );  }  factory ApiResponse.error(String msg) => ApiResponse(        success: false,        statusCode: 0,        message: msg,      );}lib/core/auth_service.dart:import 'package:flutter_secure_storage/flutter_secure_storage.dart';import 'api_client.dart';class AuthService {  static const FlutterSecureStorage _storage = FlutterSecureStorage();  static Future<ApiResponse> login(String email, String password, {String deviceName = 'Flutter App'}) async {    final res = await ApiClient.post('/auth/login', {      'email': email,      'password': password,      'device_name': deviceName,    }, auth: false);    if (res.success && res.data?['data'] != null) {      final token = res.data!['data']['token'] as String;      await _storage.write(key: 'token', value: token);      final user = res.data!['data']['user'] as Map<String, dynamic>;      await _storage.write(key: 'user_name', value: user['name'].toString());      await _storage.write(key: 'user_email', value: user['email'].toString());      await _storage.write(key: 'user_role', value: user['role'].toString());    }    return res;  }  static Future<void> logout() async {    await ApiClient.post('/auth/logout', {});    await _storage.deleteAll();  }  static Future<Map<String, String?>> getUserInfo() async {    return {      'name': await _storage.read(key: 'user_name'),      'email': await _storage.read(key: 'user_email'),      'role': await _storage.read(key: 'user_role'),    };  }  static Future<bool> isAdmin() async {    final role = await _storage.read(key: 'user_role');    return role == 'admin';  }}lib/features/auth/login_page.dart:import 'package:flutter/material.dart';import '../../core/auth_service.dart';import '../dashboard/dashboard_page.dart';class LoginPage extends StatefulWidget {  const LoginPage({super.key});  @override  State<LoginPage> createState() => _LoginPageState();}class _LoginPageState extends State<LoginPage> {  final _formKey = GlobalKey<FormState>();  final _emailCtrl = TextEditingController();  final _passwordCtrl = TextEditingController();  bool _loading = false;  bool _obscure = true;  String? _error;  Future<void> _login() async {    if (!_formKey.currentState!.validate()) return;    setState(() { _loading = true; _error = null; });    final res = await AuthService.login(      _emailCtrl.text.trim(),      _passwordCtrl.text,    );    if (!mounted) return;    setState(() => _loading = false);    if (res.success) {      Navigator.pushReplacement(        context,        MaterialPageRoute(builder: (_) => const DashboardPage()),      );    } else {      setState(() => _error = res.message.isNotEmpty ? res.message : 'Login gagal. Periksa email dan password.');    }  }  @override  void dispose() {    _emailCtrl.dispose();    _passwordCtrl.dispose();    super.dispose();  }  @override  Widget build(BuildContext context) {    return Scaffold(      backgroundColor: const Color(0xFF1E3A5F),      body: Center(        child: SingleChildScrollView(          padding: const EdgeInsets.all(24),          child: Column(            children: [              const Icon(Icons.security_rounded, size: 56, color: Colors.white),              const SizedBox(height: 12),              const Text(                'Risk Master',                style: TextStyle(fontSize: 26, fontWeight: FontWeight.bold, color: Colors.white),              ),              const Text(                'SPK Mitigasi Risiko Keamanan Informasi',                style: TextStyle(fontSize: 12, color: Colors.white60),              ),              const SizedBox(height: 32),              // Card              Container(                padding: const EdgeInsets.all(24),                decoration: BoxDecoration(                  color: Colors.white,                  borderRadius: BorderRadius.circular(16),                  boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.15), blurRadius: 20)],                ),                child: Form(                  key: _formKey,                  child: Column(                    crossAxisAlignment: CrossAxisAlignment.stretch,                    children: [                      Text(                        'Masuk ke Akun',                        style: TextStyle(                          fontSize: 18, fontWeight: FontWeight.bold,                          color: Colors.grey[900],                        ),                      ),                      const SizedBox(height: 20),                      if (_error != null) ...[                        Container(                          padding: const EdgeInsets.all(10),                          decoration: BoxDecoration(                            color: Colors.red[50],                            borderRadius: BorderRadius.circular(8),                            border: Border.all(color: Colors.red[200]!),                          ),                          child: Text(                            _error!,                            style: TextStyle(color: Colors.red[700], fontSize: 13),                          ),                        ),                        const SizedBox(height: 14),                      ],                      TextFormField(                        controller: _emailCtrl,                        keyboardType: TextInputType.emailAddress,                        decoration: const InputDecoration(                          labelText: 'Email',                          prefixIcon: Icon(Icons.email_outlined),                        ),                        validator: (v) => v == null || !v.contains('@') ? 'Email tidak valid' : null,                      ),                      const SizedBox(height: 14),                      TextFormField(                        controller: _passwordCtrl,                        obscureText: _obscure,                        decoration: InputDecoration(                          labelText: 'Password',                          prefixIcon: const Icon(Icons.lock_outlined),                          suffixIcon: IconButton(                            icon: Icon(_obscure ? Icons.visibility_off : Icons.visibility),                            onPressed: () => setState(() => _obscure = !_obscure),                          ),                        ),                        validator: (v) => v == null || v.isEmpty ? 'Password wajib diisi' : null,                      ),                      const SizedBox(height: 20),                      SizedBox(                        height: 46,                        child: ElevatedButton(                          onPressed: _loading ? null : _login,                          style: ElevatedButton.styleFrom(                            backgroundColor: const Color(0xFF1E3A5F),                            foregroundColor: Colors.white,                            shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),                          ),                          child: _loading                              ? const SizedBox(width: 20, height: 20,                                  child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))                              : const Text('Masuk', style: TextStyle(fontSize: 15, fontWeight: FontWeight.w600)),                        ),                      ),                    ],                  ),                ),              ),            ],          ),        ),      ),    );  }}lib/features/dashboard/dashboard_page.dart: import 'package:flutter/material.dart';import '../../core/auth_service.dart';import '../../core/api_client.dart';import '../assessment/assessment_list_page.dart';import '../alternatives/alternative_list_page.dart';import '../auth/login_page.dart';class DashboardPage extends StatefulWidget {  const DashboardPage({super.key});  @override  State<DashboardPage> createState() => _DashboardPageState();}class _DashboardPageState extends State<DashboardPage> {  int _currentIndex = 0;  Map<String, String?> _userInfo = {};  @override  void initState() {    super.initState();    _loadUser();  }  Future<void> _loadUser() async {    final info = await AuthService.getUserInfo();    if (mounted) setState(() => _userInfo = info);  }  Future<void> _logout() async {    await AuthService.logout();    if (!mounted) return;    Navigator.pushReplacement(      context,      MaterialPageRoute(builder: (_) => const LoginPage()),    );  }  @override  Widget build(BuildContext context) {    final pages = [      const _DashboardHomeTab(),      const AssessmentListPage(),      const AlternativeListPage(),    ];    return Scaffold(      appBar: AppBar(        title: const Text('Risk Master'),        actions: [          PopupMenuButton<String>(            icon: CircleAvatar(              backgroundColor: Colors.white24,              radius: 16,              child: Text(                (_userInfo['name'] ?? 'U').substring(0, 1).toUpperCase(),                style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold),              ),            ),            onSelected: (v) {              if (v == 'logout') _logout();            },            itemBuilder: (_) => [              PopupMenuItem(                enabled: false,                child: Column(                  crossAxisAlignment: CrossAxisAlignment.start,                  children: [                    Text(_userInfo['name'] ?? '', style: const TextStyle(fontWeight: FontWeight.bold)),                    Text(_userInfo['email'] ?? '', style: TextStyle(fontSize: 12, color: Colors.grey[600])),                    Text((_userInfo['role'] ?? '').toUpperCase(),                        style: const TextStyle(fontSize: 11, color: Colors.blue)),                  ],                ),              ),              const PopupMenuDivider(),              const PopupMenuItem(value: 'logout', child: Text('Keluar')),            ],          ),          const SizedBox(width: 8),        ],      ),      body: pages[_currentIndex],      bottomNavigationBar: NavigationBar(        selectedIndex: _currentIndex,        onDestinationSelected: (i) => setState(() => _currentIndex = i),        destinations: const [          NavigationDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home), label: 'Beranda'),          NavigationDestination(icon: Icon(Icons.assignment_outlined), selectedIcon: Icon(Icons.assignment), label: 'Assessment'),          NavigationDestination(icon: Icon(Icons.list_alt_outlined), selectedIcon: Icon(Icons.list_alt), label: 'Alternatif'),        ],      ),    );  }}class _DashboardHomeTab extends StatefulWidget {  const _DashboardHomeTab();  @override  State<_DashboardHomeTab> createState() => __DashboardHomeTabState();}class __DashboardHomeTabState extends State<_DashboardHomeTab> {  List<dynamic> _assessments = [];  bool _loading = true;  @override  void initState() {    super.initState();    _load();  }  Future<void> _load() async {    final res = await ApiClient.get('/assessments?per_page=5');    if (mounted) {      setState(() {        _loading = false;        if (res.success) {          _assessments = res.data?['data']?['assessments'] ?? [];        }      });    }  }  @override  Widget build(BuildContext context) {    if (_loading) {      return const Center(child: CircularProgressIndicator());    }    return RefreshIndicator(      onRefresh: () async { setState(() => _loading = true); await _load(); },      child: ListView(        padding: const EdgeInsets.all(16),        children: [          const Text('Assessment Terbaru', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),          const SizedBox(height: 12),          if (_assessments.isEmpty)            const Card(              child: Padding(                padding: EdgeInsets.all(24),                child: Center(child: Text('Belum ada assessment', style: TextStyle(color: Colors.grey))),              ),            )          else            ...(_assessments.map((a) => _AssessmentCard(assessment: a)).toList()),        ],      ),    );  }}class _AssessmentCard extends StatelessWidget {  final Map<String, dynamic> assessment;  const _AssessmentCard({required this.assessment});  @override  Widget build(BuildContext context) {    final isCompleted = assessment['status'] == 'completed';    return Card(      margin: const EdgeInsets.only(bottom: 10),      child: ListTile(        contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),        title: Text(          assessment['title'] ?? '',          style: const TextStyle(fontWeight: FontWeight.w600),          maxLines: 1,          overflow: TextOverflow.ellipsis,        ),        subtitle: Text(          '${assessment['alternatives_count'] ?? 0} alternatif',          style: const TextStyle(fontSize: 12),        ),        trailing: Container(          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),          decoration: BoxDecoration(            color: isCompleted ? Colors.green[50] : Colors.amber[50],            borderRadius: BorderRadius.circular(20),            border: Border.all(color: isCompleted ? Colors.green[200]! : Colors.amber[200]!),          ),          child: Text(            isCompleted ? 'Selesai' : 'Draft',            style: TextStyle(              fontSize: 11,              fontWeight: FontWeight.w600,              color: isCompleted ? Colors.green[700] : Colors.amber[700],            ),          ),        ),        onTap: () {          Navigator.push(            context,            MaterialPageRoute(              builder: (_) => AssessmentDetailNavigator(assessment: assessment),            ),          );        },      ),    );  }}// Navigator placeholder — implementasi detail di file terpisahclass AssessmentDetailNavigator extends StatelessWidget {  final Map<String, dynamic> assessment;  const AssessmentDetailNavigator({super.key, required this.assessment});  @override  Widget build(BuildContext context) {    return Scaffold(      appBar: AppBar(title: Text(assessment['title'] ?? '')),      body: const Center(child: Text('Assessment detail')),    );  }}lib/features/assessment/assessment_list_page.dart:import 'package:flutter/material.dart';import '../../core/api_client.dart';import 'assessment_detail_page.dart';import 'create_assessment_page.dart';class AssessmentListPage extends StatefulWidget {  const AssessmentListPage({super.key});  @override  State<AssessmentListPage> createState() => _AssessmentListPageState();}class _AssessmentListPageState extends State<AssessmentListPage> {  List<dynamic> _assessments = [];  bool _loading = true;  String _statusFilter = '';  final _searchCtrl = TextEditingController();  @override  void initState() {    super.initState();    _load();  }  Future<void> _load() async {    setState(() => _loading = true);    final query = [      if (_statusFilter.isNotEmpty) 'status=$_statusFilter',      if (_searchCtrl.text.isNotEmpty) 'search=${_searchCtrl.text}',    ].join('&');    final res = await ApiClient.get('/assessments?$query');    if (mounted) {      setState(() {        _loading = false;        _assessments = res.success ? (res.data?['data']?['assessments'] ?? []) : [];      });    }  }  @override  Widget build(BuildContext context) {    return Scaffold(      floatingActionButton: FloatingActionButton.extended(        onPressed: () async {          final created = await Navigator.push(            context,            MaterialPageRoute(builder: (_) => const CreateAssessmentPage()),          );          if (created == true) _load();        },        icon: const Icon(Icons.add),        label: const Text('Buat'),      ),      body: Column(        children: [          Padding(            padding: const EdgeInsets.all(12),            child: Row(              children: [                Expanded(                  child: TextField(                    controller: _searchCtrl,                    decoration: const InputDecoration(                      hintText: 'Cari assessment...',                      prefixIcon: Icon(Icons.search, size: 20),                      isDense: true,                    ),                    onSubmitted: (_) => _load(),                  ),                ),                const SizedBox(width: 8),                DropdownButtonHideUnderline(                  child: DropdownButton<String>(                    value: _statusFilter.isEmpty ? null : _statusFilter,                    hint: const Text('Status'),                    items: [                      const DropdownMenuItem(value: '', child: Text('Semua')),                      const DropdownMenuItem(value: 'draft', child: Text('Draft')),                      const DropdownMenuItem(value: 'completed', child: Text('Selesai')),                    ],                    onChanged: (v) {                      setState(() => _statusFilter = v ?? '');                      _load();                    },                  ),                ),              ],            ),          ),          Expanded(            child: _loading                ? const Center(child: CircularProgressIndicator())                : RefreshIndicator(                    onRefresh: _load,                    child: _assessments.isEmpty                        ? const Center(child: Text('Tidak ada assessment', style: TextStyle(color: Colors.grey)))                        : ListView.builder(                            padding: const EdgeInsets.fromLTRB(12, 0, 12, 80),                            itemCount: _assessments.length,                            itemBuilder: (ctx, i) {                              final a = _assessments[i] as Map<String, dynamic>;                              final isDone = a['status'] == 'completed';                              return Card(                                margin: const EdgeInsets.only(bottom: 10),                                child: ListTile(                                  contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),                                  title: Text(                                    a['title'] ?? '',                                    style: const TextStyle(fontWeight: FontWeight.w600),                                    maxLines: 2,                                    overflow: TextOverflow.ellipsis,                                  ),                                  subtitle: Padding(                                    padding: const EdgeInsets.only(top: 4),                                    child: Row(                                      children: [                                        Icon(Icons.account_tree_outlined, size: 13, color: Colors.grey[500]),                                        const SizedBox(width: 4),                                        Text('${a['alternatives_count'] ?? 0} alternatif',                                            style: const TextStyle(fontSize: 12)),                                      ],                                    ),                                  ),                                  trailing: Container(                                    padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),                                    decoration: BoxDecoration(                                      color: isDone ? Colors.green[50] : Colors.amber[50],                                      borderRadius: BorderRadius.circular(20),                                      border: Border.all(color: isDone ? Colors.green[200]! : Colors.amber[200]!),                                    ),                                    child: Text(                                      isDone ? 'Selesai' : 'Draft',                                      style: TextStyle(                                        fontSize: 11,                                        fontWeight: FontWeight.w600,                                        color: isDone ? Colors.green[700] : Colors.amber[700],                                      ),                                    ),                                  ),                                  onTap: () async {                                    final updated = await Navigator.push(                                      context,                                      MaterialPageRoute(                                        builder: (_) => AssessmentDetailPage(assessmentId: a['id'].toString()),                                      ),                                    );                                    if (updated == true) _load();                                  },                                ),                              );                            },                          ),                  ),          ),        ],      ),    );  }}lib/features/assessment/assessment_detail_page.dart:import 'package:flutter/material.dart';import '../../core/api_client.dart';import 'edas_result_page.dart';class AssessmentDetailPage extends StatefulWidget {  final String assessmentId;  const AssessmentDetailPage({super.key, required this.assessmentId});  @override  State<AssessmentDetailPage> createState() => _AssessmentDetailPageState();}class _AssessmentDetailPageState extends State<AssessmentDetailPage> {  Map<String, dynamic>? _data;  bool _loading = true;  bool _calculating = false;  @override  void initState() {    super.initState();    _load();  }  Future<void> _load() async {    setState(() => _loading = true);    final res = await ApiClient.get('/assessments/${widget.assessmentId}');    if (mounted) {      setState(() {        _loading = false;        _data = res.success ? res.data?['data'] : null;      });    }  }  Future<void> _calculate() async {    setState(() => _calculating = true);    final res = await ApiClient.post(        '/assessments/${widget.assessmentId}/calculate', {});    setState(() => _calculating = false);    if (!mounted) return;    if (res.success) {      ScaffoldMessenger.of(context).showSnackBar(        const SnackBar(content: Text('Kalkulasi EDAS berhasil!'), backgroundColor: Colors.green),      );      await _load();    } else {      ScaffoldMessenger.of(context).showSnackBar(        SnackBar(content: Text(res.message), backgroundColor: Colors.red),      );    }  }  @override  Widget build(BuildContext context) {    if (_loading) {      return const Scaffold(body: Center(child: CircularProgressIndicator()));    }    if (_data == null) {      return Scaffold(        appBar: AppBar(title: const Text('Assessment')),        body: const Center(child: Text('Gagal memuat data')),      );    }    final assessment = _data!['assessment'] as Map<String, dynamic>;    final criteria = _data!['criteria'] as List? ?? [];    final alternatives = (assessment['alternatives'] as List?) ?? [];    final isCompleted = assessment['status'] == 'completed';    final isComplete = _data!['is_complete'] == true;    final filled = _data!['filled_count'] ?? 0;    final expected = _data!['expected_count'] ?? 0;    return Scaffold(      appBar: AppBar(        title: Text(assessment['title'] ?? '', overflow: TextOverflow.ellipsis),        actions: [          if (isCompleted)            TextButton(              onPressed: () {                Navigator.push(                  context,                  MaterialPageRoute(                    builder: (_) => EdasResultPage(assessmentId: widget.assessmentId),                  ),                );              },              child: const Text('Hasil', style: TextStyle(color: Colors.white)),            ),        ],      ),      body: RefreshIndicator(        onRefresh: _load,        child: ListView(          padding: const EdgeInsets.all(16),          children: [            // Status badge            Row(              children: [                Container(                  padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),                  decoration: BoxDecoration(                    color: isCompleted ? Colors.green[50] : Colors.amber[50],                    borderRadius: BorderRadius.circular(20),                    border: Border.all(color: isCompleted ? Colors.green[200]! : Colors.amber[200]!),                  ),                  child: Text(                    isCompleted ? 'Selesai' : 'Draft',                    style: TextStyle(                      fontSize: 12,                      fontWeight: FontWeight.w600,                      color: isCompleted ? Colors.green[700] : Colors.amber[700],                    ),                  ),                ),                const SizedBox(width: 8),                Text(assessment['owner']?['name'] ?? '',                    style: TextStyle(fontSize: 12, color: Colors.grey[600])),              ],            ),            if (assessment['description'] != null) ...[              const SizedBox(height: 8),              Text(assessment['description'], style: TextStyle(color: Colors.grey[700])),            ],            const SizedBox(height: 16),            // Matrix progress            Card(              child: Padding(                padding: const EdgeInsets.all(16),                child: Column(                  crossAxisAlignment: CrossAxisAlignment.start,                  children: [                    Row(                      mainAxisAlignment: MainAxisAlignment.spaceBetween,                      children: [                        const Text('Progres Matrix', style: TextStyle(fontWeight: FontWeight.w600)),                        Text('$filled/$expected', style: TextStyle(                          color: isComplete ? Colors.green : Colors.grey[600],                          fontWeight: FontWeight.w600,                        )),                      ],                    ),                    const SizedBox(height: 8),                    LinearProgressIndicator(                      value: expected > 0 ? filled / expected : 0,                      backgroundColor: Colors.grey[200],                      color: isComplete ? Colors.green : Colors.blue,                      minHeight: 6,                      borderRadius: BorderRadius.circular(3),                    ),                    if (!isComplete && !isCompleted)                      Padding(                        padding: const EdgeInsets.only(top: 6),                        child: Text(                          'Masih ${expected - filled} sel belum diisi',                          style: TextStyle(fontSize: 12, color: Colors.amber[700]),                        ),                      ),                  ],                ),              ),            ),            const SizedBox(height: 12),            // Alternatives list            Card(              child: Padding(                padding: const EdgeInsets.all(16),                child: Column(                  crossAxisAlignment: CrossAxisAlignment.start,                  children: [                    Text('Alternatif (${alternatives.length})',                        style: const TextStyle(fontWeight: FontWeight.w600)),                    const SizedBox(height: 8),                    if (alternatives.isEmpty)                      Text('Belum ada alternatif', style: TextStyle(color: Colors.grey[500]))                    else                      ...alternatives.map<Widget>((a) => Padding(                        padding: const EdgeInsets.symmetric(vertical: 4),                        child: Row(                          children: [                            Icon(Icons.circle, size: 6, color: Colors.grey[400]),                            const SizedBox(width: 8),                            Expanded(child: Text(a['name'] ?? '', style: const TextStyle(fontSize: 14))),                          ],                        ),                      )),                  ],                ),              ),            ),            const SizedBox(height: 12),            // Criteria            Card(              child: Padding(                padding: const EdgeInsets.all(16),                child: Column(                  crossAxisAlignment: CrossAxisAlignment.start,                  children: [                    Text('Kriteria EDAS (${criteria.length})',                        style: const TextStyle(fontWeight: FontWeight.w600)),                    const SizedBox(height: 8),                    ...criteria.map<Widget>((c) {                      final isBenefit = c['type'] == 'benefit';                      return Padding(                        padding: const EdgeInsets.symmetric(vertical: 4),                        child: Row(                          children: [                            Container(                              padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),                              decoration: BoxDecoration(                                color: isBenefit ? Colors.blue[50] : Colors.orange[50],                                borderRadius: BorderRadius.circular(4),                              ),                              child: Text(                                (c['type'] ?? '').toString().toUpperCase(),                                style: TextStyle(                                  fontSize: 10,                                  fontWeight: FontWeight.bold,                                  color: isBenefit ? Colors.blue[700] : Colors.orange[700],                                ),                              ),                            ),                            const SizedBox(width: 8),                            Expanded(child: Text(c['name'] ?? '', style: const TextStyle(fontSize: 13))),                            Text('${((c['weight'] ?? 0) * 100).toStringAsFixed(1)}%',                                style: TextStyle(fontSize: 12, color: Colors.grey[500])),                          ],                        ),                      );                    }),                  ],                ),              ),            ),            const SizedBox(height: 20),            // Actions            if (!isCompleted) ...[              if (isComplete)                ElevatedButton.icon(                  onPressed: _calculating ? null : _calculate,                  icon: _calculating                      ? const SizedBox(width: 16, height: 16, child: CircularProgressIndicator(strokeWidth: 2))                      : const Icon(Icons.calculate_outlined),                  label: const Text('Hitung EDAS'),                  style: ElevatedButton.styleFrom(                    backgroundColor: const Color(0xFF1E3A5F),                    foregroundColor: Colors.white,                    minimumSize: const Size.fromHeight(46),                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),                  ),                )              else                OutlinedButton.icon(                  onPressed: null,                  icon: const Icon(Icons.calculate_outlined),                  label: Text('Hitung EDAS (isi nilai terlebih dahulu: $filled/$expected)'),                  style: OutlinedButton.styleFrom(minimumSize: const Size.fromHeight(46)),                ),            ] else ...[              ElevatedButton.icon(                onPressed: () {                  Navigator.push(                    context,                    MaterialPageRoute(                      builder: (_) => EdasResultPage(assessmentId: widget.assessmentId),                    ),                  );                },                icon: const Icon(Icons.bar_chart_outlined),                label: const Text('Lihat Hasil EDAS'),                style: ElevatedButton.styleFrom(                  backgroundColor: Colors.green[700],                  foregroundColor: Colors.white,                  minimumSize: const Size.fromHeight(46),                  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),                ),              ),            ],          ],        ),      ),    );  }}lib/features/assessment/edas_result_page.dart:import 'package:flutter/material.dart';import '../../core/api_client.dart';class EdasResultPage extends StatefulWidget {  final String assessmentId;  const EdasResultPage({super.key, required this.assessmentId});  @override  State<EdasResultPage> createState() => _EdasResultPageState();}class _EdasResultPageState extends State<EdasResultPage> {  Map<String, dynamic>? _data;  bool _loading = true;  @override  void initState() {    super.initState();    _load();  }  Future<void> _load() async {    final res = await ApiClient.get('/assessments/${widget.assessmentId}/results');    if (mounted) {      setState(() {        _loading = false;        _data = res.success ? res.data?['data'] : null;      });    }  }  Color _qualityColor(String? label) {    switch (label) {      case 'Sangat Direkomendasikan': return Colors.green;      case 'Direkomendasikan': return Colors.blue;      case 'Cukup': return Colors.amber;      default: return Colors.red;    }  }  @override  Widget build(BuildContext context) {    if (_loading) {      return const Scaffold(body: Center(child: CircularProgressIndicator()));    }    if (_data == null) {      return Scaffold(        appBar: AppBar(title: const Text('Hasil EDAS')),        body: const Center(child: Text('Gagal memuat hasil')),      );    }    final assessment = _data!['assessment'] as Map<String, dynamic>;    final results = _data!['results'] as List? ?? [];    final topRec = _data!['top_recommendation'] as String?;    return Scaffold(      appBar: AppBar(        title: Text(assessment['title'] ?? '', overflow: TextOverflow.ellipsis),      ),      body: ListView(        padding: const EdgeInsets.all(16),        children: [          // Top recommendation          if (topRec != null)            Container(              padding: const EdgeInsets.all(16),              decoration: BoxDecoration(                gradient: const LinearGradient(                  colors: [Color(0xFF1E3A5F), Color(0xFF3B4F7C)],                ),                borderRadius: BorderRadius.circular(14),              ),              child: Column(                crossAxisAlignment: CrossAxisAlignment.start,                children: [                  const Text('🏆 Rekomendasi Utama',                      style: TextStyle(color: Colors.white70, fontSize: 12)),                  const SizedBox(height: 4),                  Text(topRec,                      style: const TextStyle(                          color: Colors.white, fontSize: 18, fontWeight: FontWeight.bold)),                ],              ),            ),          const SizedBox(height: 16),          const Text('Peringkat Lengkap', style: TextStyle(fontSize: 15, fontWeight: FontWeight.bold)),          const SizedBox(height: 10),          ...results.asMap().entries.map((e) {            final r = e.value as Map<String, dynamic>;            final rank = r['rank'] as int;            final alt = r['alternative'] as Map<String, dynamic>?;            final score = (r['as_score'] as num?)?.toDouble() ?? 0;            final quality = r['quality_label'] as String?;            final color = _qualityColor(quality);            return Card(              margin: const EdgeInsets.only(bottom: 10),              child: Padding(                padding: const EdgeInsets.all(14),                child: Column(                  children: [                    Row(                      children: [                        Container(                          width: 32,                          height: 32,                          decoration: BoxDecoration(                            color: rank == 1 ? const Color(0xFF1E3A5F) : Colors.grey[100],                            shape: BoxShape.circle,                          ),                          child: Center(                            child: Text(                              rank.toString(),                              style: TextStyle(                                color: rank == 1 ? Colors.white : Colors.grey[700],                                fontWeight: FontWeight.bold,                                fontSize: 14,                              ),                            ),                          ),                        ),                        const SizedBox(width: 12),                        Expanded(                          child: Text(                            alt?['name'] ?? '',                            style: const TextStyle(fontWeight: FontWeight.w600, fontSize: 14),                          ),                        ),                        Container(                          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 3),                          decoration: BoxDecoration(                            color: color.withOpacity(0.1),                            borderRadius: BorderRadius.circular(20),                            border: Border.all(color: color.withOpacity(0.3)),                          ),                          child: Text(                            quality ?? '',                            style: TextStyle(fontSize: 10, color: color, fontWeight: FontWeight.w600),                          ),                        ),                      ],                    ),                    const SizedBox(height: 12),                    // Score bar                    Row(                      children: [                        const Text('AS:', style: TextStyle(fontSize: 12, fontWeight: FontWeight.w600)),                        const SizedBox(width: 8),                        Expanded(                          child: LinearProgressIndicator(                            value: score.clamp(0.0, 1.0),                            backgroundColor: Colors.grey[200],                            color: color,                            minHeight: 6,                            borderRadius: BorderRadius.circular(3),                          ),                        ),                        const SizedBox(width: 8),                        Text(score.toStringAsFixed(4),                            style: const TextStyle(fontSize: 12, fontFamily: 'monospace', fontWeight: FontWeight.bold)),                      ],                    ),                    const SizedBox(height: 8),                    // Sub-scores                    Row(                      children: [                        _ScoreChip(label: 'NSP', value: (r['nsp'] as num?)?.toDouble()),                        const SizedBox(width: 6),                        _ScoreChip(label: 'NSN', value: (r['nsn'] as num?)?.toDouble()),                        const SizedBox(width: 6),                        _ScoreChip(label: 'SP', value: (r['sp'] as num?)?.toDouble()),                        const SizedBox(width: 6),                        _ScoreChip(label: 'SN', value: (r['sn'] as num?)?.toDouble()),                      ],                    ),                  ],                ),              ),            );          }),          const SizedBox(height: 16),          // Legend          Card(            color: Colors.grey[50],            child: Padding(              padding: const EdgeInsets.all(14),              child: Column(                crossAxisAlignment: CrossAxisAlignment.start,                children: [                  const Text('Keterangan', style: TextStyle(fontWeight: FontWeight.w600, fontSize: 13)),                  const SizedBox(height: 8),                  _LegendItem(color: Colors.green, label: 'Sangat Direkomendasikan', score: '≥ 0.80'),                  _LegendItem(color: Colors.blue, label: 'Direkomendasikan', score: '≥ 0.60'),                  _LegendItem(color: Colors.amber, label: 'Cukup', score: '≥ 0.40'),                  _LegendItem(color: Colors.red, label: 'Tidak Direkomendasikan', score: '< 0.40'),                ],              ),            ),          ),        ],      ),    );  }}class _ScoreChip extends StatelessWidget {  final String label;  final double? value;  const _ScoreChip({required this.label, this.value});  @override  Widget build(BuildContext context) {    return Expanded(      child: Container(        padding: const EdgeInsets.symmetric(vertical: 5),        decoration: BoxDecoration(          color: Colors.grey[100],          borderRadius: BorderRadius.circular(6),        ),        child: Column(          children: [            Text(label, style: TextStyle(fontSize: 10, color: Colors.grey[600])),            Text(              value?.toStringAsFixed(4) ?? '—',              style: const TextStyle(fontSize: 10, fontFamily: 'monospace', fontWeight: FontWeight.bold),            ),          ],        ),      ),    );  }}class _LegendItem extends StatelessWidget {  final Color color;  final String label;  final String score;  const _LegendItem({required this.color, required this.label, required this.score});  @override  Widget build(BuildContext context) {    return Padding(      padding: const EdgeInsets.only(bottom: 5),      child: Row(        children: [          Container(width: 10, height: 10, decoration: BoxDecoration(color: color, shape: BoxShape.circle)),          const SizedBox(width: 8),          Text(label, style: const TextStyle(fontSize: 12)),          const Spacer(),          Text(score, style: TextStyle(fontSize: 11, color: Colors.grey[600])),        ],      ),    );  }}lib/features/assessment/create_assessment_page.dart:import 'package:flutter/material.dart';import '../../core/api_client.dart';class CreateAssessmentPage extends StatefulWidget {  const CreateAssessmentPage({super.key});  @override  State<CreateAssessmentPage> createState() => _CreateAssessmentPageState();}class _CreateAssessmentPageState extends State<CreateAssessmentPage> {  final _formKey = GlobalKey<FormState>();  final _titleCtrl = TextEditingController();  final _descCtrl = TextEditingController();  List<dynamic> _alternatives = [];  Set<int> _selectedIds = {};  bool _loading = false;  bool _loadingAlts = true;  @override  void initState() {    super.initState();    _loadAlternatives();  }  Future<void> _loadAlternatives() async {    final res = await ApiClient.get('/alternatives?per_page=100');    if (mounted) {      setState(() {        _loadingAlts = false;        _alternatives = res.success ? (res.data?['data']?['alternatives'] ?? []) : [];      });    }  }  Future<void> _submit() async {    if (!_formKey.currentState!.validate()) return;    setState(() => _loading = true);    final res = await ApiClient.post('/assessments', {      'title': _titleCtrl.text.trim(),      'description': _descCtrl.text.trim().isEmpty ? null : _descCtrl.text.trim(),      'alternative_ids': _selectedIds.toList(),    });    setState(() => _loading = false);    if (!mounted) return;    if (res.success) {      Navigator.pop(context, true);    } else {      ScaffoldMessenger.of(context).showSnackBar(        SnackBar(content: Text(res.message), backgroundColor: Colors.red),      );    }  }  @override  Widget build(BuildContext context) {    return Scaffold(      appBar: AppBar(title: const Text('Buat Assessment')),      body: Form(        key: _formKey,        child: ListView(          padding: const EdgeInsets.all(16),          children: [            TextFormField(              controller: _titleCtrl,              decoration: const InputDecoration(labelText: 'Judul Assessment *'),              validator: (v) => v == null || v.trim().isEmpty ? 'Judul wajib diisi' : null,            ),            const SizedBox(height: 12),            TextFormField(              controller: _descCtrl,              decoration: const InputDecoration(labelText: 'Deskripsi (opsional)'),              maxLines: 3,            ),            const SizedBox(height: 20),            const Text('Pilih Alternatif (opsional)',                style: TextStyle(fontWeight: FontWeight.w600, fontSize: 14)),            const SizedBox(height: 8),            if (_loadingAlts)              const Center(child: CircularProgressIndicator())            else if (_alternatives.isEmpty)              Text('Tidak ada alternatif tersedia', style: TextStyle(color: Colors.grey[500]))            else              Card(                child: Column(                  children: _alternatives.map<Widget>((a) {                    final id = a['id'] as int;                    return CheckboxListTile(                      title: Text(a['name'] ?? '', style: const TextStyle(fontSize: 14)),                      subtitle: a['description'] != null                          ? Text(a['description'], maxLines: 1, overflow: TextOverflow.ellipsis,                              style: const TextStyle(fontSize: 12))                          : null,                      value: _selectedIds.contains(id),                      onChanged: (v) {                        setState(() {                          if (v == true) _selectedIds.add(id);                          else _selectedIds.remove(id);                        });                      },                      dense: true,                    );                  }).toList(),                ),              ),            const SizedBox(height: 24),            ElevatedButton(              onPressed: _loading ? null : _submit,              style: ElevatedButton.styleFrom(                backgroundColor: const Color(0xFF1E3A5F),                foregroundColor: Colors.white,                minimumSize: const Size.fromHeight(46),                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),              ),              child: _loading                  ? const SizedBox(width: 20, height: 20,                      child: CircularProgressIndicator(strokeWidth: 2, color: Colors.white))                  : const Text('Buat Assessment', style: TextStyle(fontSize: 15)),            ),          ],        ),      ),    );  }}lib/features/alternatives/alternative_list_page.dart:import 'package:flutter/material.dart';import '../../core/api_client.dart';class AlternativeListPage extends StatefulWidget {  const AlternativeListPage({super.key});  @override  State<AlternativeListPage> createState() => _AlternativeListPageState();}class _AlternativeListPageState extends State<AlternativeListPage> {  List<dynamic> _alternatives = [];  bool _loading = true;  final _searchCtrl = TextEditingController();  @override  void initState() {    super.initState();    _load();  }  Future<void> _load() async {    setState(() => _loading = true);    final q = _searchCtrl.text.isNotEmpty ? '?search=${_searchCtrl.text}' : '';    final res = await ApiClient.get('/alternatives$q');    if (mounted) {      setState(() {        _loading = false;        _alternatives = res.success ? (res.data?['data']?['alternatives'] ?? []) : [];      });    }  }  Future<void> _addAlternative() async {    final nameCtrl = TextEditingController();    final descCtrl = TextEditingController();    await showDialog(      context: context,      builder: (ctx) => AlertDialog(        title: const Text('Tambah Alternatif'),        content: Column(          mainAxisSize: MainAxisSize.min,          children: [            TextField(              controller: nameCtrl,              decoration: const InputDecoration(labelText: 'Nama *'),            ),            const SizedBox(height: 8),            TextField(              controller: descCtrl,              decoration: const InputDecoration(labelText: 'Deskripsi'),              maxLines: 2,            ),          ],        ),        actions: [          TextButton(onPressed: () => Navigator.pop(ctx), child: const Text('Batal')),          ElevatedButton(            onPressed: () async {              if (nameCtrl.text.trim().isEmpty) return;              Navigator.pop(ctx);              final res = await ApiClient.post('/alternatives', {                'name': nameCtrl.text.trim(),                'description': descCtrl.text.trim().isEmpty ? null : descCtrl.text.trim(),              });              if (res.success) {                _load();              } else if (mounted) {                ScaffoldMessenger.of(context).showSnackBar(                  SnackBar(content: Text(res.message), backgroundColor: Colors.red),                );              }            },            child: const Text('Tambah'),          ),        ],      ),    );  }  @override  Widget build(BuildContext context) {    return Scaffold(      floatingActionButton: FloatingActionButton.extended(        onPressed: _addAlternative,        icon: const Icon(Icons.add),        label: const Text('Tambah'),      ),      body: Column(        children: [          Padding(            padding: const EdgeInsets.all(12),            child: TextField(              controller: _searchCtrl,              decoration: const InputDecoration(                hintText: 'Cari alternatif...',                prefixIcon: Icon(Icons.search, size: 20),                isDense: true,              ),              onSubmitted: (_) => _load(),            ),          ),          Expanded(            child: _loading                ? const Center(child: CircularProgressIndicator())                : RefreshIndicator(                    onRefresh: _load,                    child: _alternatives.isEmpty                        ? const Center(child: Text('Tidak ada alternatif', style: TextStyle(color: Colors.grey)))                        : ListView.builder(                            padding: const EdgeInsets.fromLTRB(12, 0, 12, 80),                            itemCount: _alternatives.length,                            itemBuilder: (ctx, i) {                              final a = _alternatives[i] as Map<String, dynamic>;                              final isAdmin = a['source'] == 'admin';                              return Card(                                margin: const EdgeInsets.only(bottom: 8),                                child: ListTile(                                  title: Text(a['name'] ?? ''),                                  subtitle: a['description'] != null                                      ? Text(a['description'], maxLines: 2, overflow: TextOverflow.ellipsis)                                      : null,                                  trailing: Container(                                    padding: const EdgeInsets.symmetric(horizontal: 7, vertical: 3),                                    decoration: BoxDecoration(                                      color: isAdmin ? Colors.blue[50] : Colors.purple[50],                                      borderRadius: BorderRadius.circular(10),                                    ),                                    child: Text(                                      isAdmin ? 'Admin' : 'User',                                      style: TextStyle(                                        fontSize: 10,                                        color: isAdmin ? Colors.blue[700] : Colors.purple[700],                                        fontWeight: FontWeight.w600,                                      ),                                    ),                                  ),                                ),                              );                            },                          ),                  ),          ),        ],      ),    );  }}
